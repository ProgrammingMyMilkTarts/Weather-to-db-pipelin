# Weather-to-db-pipelin
Light weight, extensible python automation script to that queries a live weather API, parses the JSON payload, and logs historical weather metric into local file storage and a containerized relational database. Used to practice API, data structuring and local db management

Lang: Python
Database: PostgreSQL (Dockerized)
